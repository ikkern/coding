# List of colors
colors = ['red', 'blue', 'green', 'yellow', 'purple']

# Slice to get the second and third colors
sliced_colors = colors[1:3]
print(sliced_colors)  # ['blue', 'green']

# Print the length of the original list
print(len(colors))  # 5